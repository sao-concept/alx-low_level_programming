#include <stdio.h>
/**
 * main -Entry point
 * ------------------
 * Return: Always 0 (Success)
 */
int main(void)
{
	int i = 0;

	while (i > 0)
	{
		printf("%d is a base_10_number\n", i);
		i++;
	}
	return (0);
}
